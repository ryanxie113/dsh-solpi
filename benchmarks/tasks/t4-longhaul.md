项目位于 /tmp/bench-t4（已存在，勿重建）：12 个 src 模块各含 3 个带 bug 的函数，test/ 下有对应的 12 个测试文件，共 36 个用例，当前几乎全部失败。

任务：逐一修复全部 12 个模块。对每个模块严格执行以下循环：

1. 运行 `node --experimental-strip-types --test test/<module>.test.ts` 查看该模块完整失败输出
2. 分析失败详情，修复 src/<module>.ts 中对应函数（不许改 test 文件；不许删除或跳过用例）
3. 复跑同一命令确认 pass 3 / fail 0 后才进入下一个模块

约束：
- 必须按模块逐个处理（先 vector 再 matrix 依此类推），禁止一次性改所有模块后再统一验证
- 若同一函数连续修复 3 次仍失败，标记 SKIP 并继续下一个，不要死磕
- 全部处理完后运行完整 `npm test`，最后报告一张表：模块名 | 修复了什么 | 最终状态（pass/SKIP）
